# AI Sales Machine — multilingual demo

Languages: Ukrainian (`/uk/`), English (`/en/`), Polish (`/pl/`).
Each language has its own page and language switcher. The demo gives access to one random prompt from three demo topics.
